# Install dependencies
install:
    pip install -r requirements.txt

# Check code formatting with black
format:
    black . --check

# Run tests
test:
    pytest tests/

# Train the model
train:
    python train.py

# Evaluate the model and log the results
eval:
    python evaluate.py > metrics.txt
    cat metrics.txt  # Print metrics to the console
